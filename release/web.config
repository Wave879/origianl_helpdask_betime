<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <security>
      <requestFiltering>
        <requestLimits maxAllowedContentLength="524288000" />
      </requestFiltering>
    </security>
    <serverRuntime uploadReadAheadSize="1048576" />
    <rewrite>
      <rules>
        <rule name="Normalize web prefix" stopProcessing="true">
          <match url="^web$" />
          <action type="Redirect" url="/web/" redirectType="Found" />
        </rule>
        <rule name="Serve web landing page" stopProcessing="true">
          <match url="^web/?$" />
          <action type="Rewrite" url="http://192.168.100.12:19120/web/" logRewrittenUrl="true" />
        </rule>
        <rule name="Redirect Dify editor legacy routes" stopProcessing="true">
          <match url="^web/app/([0-9a-f-]+)/(workflow|configuration|overview)$" />
          <conditions>
            <add input="{HTTP_COOKIE}" pattern="__Host-access_token=" />
          </conditions>
          <action type="Redirect" url="/dify/app/{R:1}/{R:2}" redirectType="Found" />
        </rule>
        <rule name="Proxy web app" stopProcessing="true">
          <match url="^web/(.*)" />
          <action type="Rewrite" url="http://192.168.100.12:19120/web/{R:1}" logRewrittenUrl="true" />
        </rule>
        <rule name="Proxy Betime root files" stopProcessing="true">
          <match url="^(assets/betime_solution/.*|shared\.js|config\.js)$" />
          <action type="Rewrite" url="http://192.168.100.12:19120/{R:1}" logRewrittenUrl="true" />
        </rule>
        <rule name="Redirect Betime root html pages to canonical web routes" stopProcessing="true">
          <match url="^(.+)\.html$" />
          <action type="Redirect" url="/web/{R:1}" redirectType="Found" />
        </rule>
        <rule name="Redirect root to Betime web" stopProcessing="true">
          <match url="^$" />
          <action type="Redirect" url="/web/" redirectType="Found" />
        </rule>
      </rules>
    </rewrite>
    <proxy reverseRewriteHostInResponseHeaders="false" preserveHostHeader="true" />
    <httpErrors existingResponse="PassThrough" />
  </system.webServer>
</configuration>
